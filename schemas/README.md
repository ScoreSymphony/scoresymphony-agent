# Schemas

Machine-readable command, event, review, artifact and configuration schemas will live here as they become canonical.

Pydantic models are the current executable bootstrap contract. JSON Schema exports may be generated from those models; they must not become a competing semantic definition.
