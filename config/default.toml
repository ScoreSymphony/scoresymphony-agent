# Non-secret reference configuration. Runtime environment variables override these concepts.
environment = "development"
state_dir = "/var/lib/scoresymphony-agent"
host = "0.0.0.0"
port = 8000

[limits]
max_review_rounds = 3
max_active_agents = 2
max_concurrent_heavy_jobs = 1
